{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"kplot","text":"<p>This library is used to draw figures for papers using the matplotlib module.</p> <p>matplotlib\u3092\u4f7f\u3063\u305f\u8ad6\u6587\u4f5c\u6210\u6642\u306b\u4f7f\u3048\u305d\u3046\u306a\u30b7\u30f3\u30d7\u30eb\u306a\u30b0\u30e9\u30d5\u3084\u30d2\u30b9\u30c8\u30b0\u30e9\u30e0\u3092\u66f8\u304f\u4e8b\u304c\u3067\u304d\u308b\u3001\u304b\u3086\u3044\u3068\u3053\u308d\u306b\u624b\u304c\u5c4a\u304f\u30e9\u30a4\u30d6\u30e9\u30ea\u3067\u3059\u3002</p> <p> </p>"},{"location":"#demo-sample","title":"demo sample","text":"<ul> <li>Scatter2D (scatter mode)</li> </ul> <ul> <li>From Tab.1 and Fig.3 data of Kamiokande experiment in ref: K. Hirata, Phys. Rev. Lett. 58, 1490</li> </ul>"},{"location":"#how-to-install","title":"How to install","text":"<ol> <li>clone this repository.</li> </ol> <p><code>bash  ~$ cd [work path]  [work path]$ git clone https://github.com/kyasya/kplot.git</code></p> <ol> <li>open the terminal, and change to <code>src</code> directory under the installed repository.</li> </ol> <p><code>bash  [work path]$ cd src</code></p> <ol> <li>install using <code>src</code> command.:</li> </ol> <p><code>bash  [work path]/bin$ pip install .</code></p> <ol> <li>Installation check. You can use the demo programs. (./samples)</li> </ol>"},{"location":"#note","title":"Note","text":"<p>This software is developing as the beta version. The program works correctly, however we have not optimized the function. We plan to support this in the next version.</p>"},{"location":"#how-to-use","title":"how to use","text":"<p>see the reference.</p>"},{"location":"#histogram","title":"histogram","text":"<pre><code>import numpy as np\nimport kplot\n\n# data\nx = np.random.normal(0, 1, 1000)\n\n# plot\nh = kplot.hist('Title;x;Events', 100, -5, 5)\nh.Draw1D(x)\n</code></pre>"},{"location":"#separated-graph","title":"Separated graph","text":"<ul> <li>This is useful when drawing a graph of the fitting.</li> </ul> <p>NOTE: Filling in the fitting caves requires a graph to be obtained. Fitting functionality will be added in the future.</p> <p></p>"},{"location":"#2d-scatterhist","title":"2D scatter/hist","text":"<ul> <li>mode change to scatter/hist.</li> </ul> <pre><code># data\nx = np.random.normal(0, 0.7, 1000)\ny = np.random.normal(0, 0.8, 1000)\n\n# plot histogram\nh = kplot.scatter3(100, -5, 5, 100, -5, 5, figsize=(6,6))\nh.Draw(x, y, mode='hist') \nh.SetxLimit(-3, 3)\nh.SetyLimit(-3, 3)\nh.SetxLabel('x')\nh.SetyLabel('y')\n\nh.Print('./demo-hist2D.png')\n</code></pre> <ul> <li>hist mode (<code>mode</code> parameter set to be <code>'hist'</code>)</li> </ul> <ul> <li>scatter mode (<code>mode</code> parameter set to be <code>'scatter'</code>)</li> </ul>"},{"location":"#license","title":"License","text":"<p>We use the MIT License.</p>"},{"location":"api/kplot/","title":"API Reference","text":""},{"location":"api/kplot/#main","title":"Main","text":"<p>kplot.hist</p> <ul> <li>The histogram and graph module using matplotlib. It's beta version. We will add the ability to manage common parameters.</li> </ul>"},{"location":"api/kplot/#kplot.kplot.hist","title":"<code>hist</code>","text":"<p>kplot.hist class</p> <p>Create a one or two dimension graph/histogram.</p> Source code in <code>kplot\\kplot.py</code> <pre><code>class hist:\n    \"\"\"kplot.hist class\n\n    Create a one or two dimension graph/histogram.\n\n    \"\"\"\n    HistXRange = None\n    uHistYRange = None\n    bHistYRange = None\n    Titles = \"\"\n    TitleLabel = \"\"\n    xAxisLabel = \"x\"\n    yAxisLabel = \"y\"\n    y2AxisLabel = \"y2\"\n\n    Fig = None\n    FigSize=(11.69,8.27)\n    Axs = []\n\n\n    def __init__(self, titles, xBin=10, xMin=0, xMax=1, yBin=0, yMin=0, yMax=0):\n        \"\"\"default constructor\n\n        default constructor\n\n        Args:\n            titles (str): The titles of the graph. \"Graph title;x-axis label;y-axis label\".\n            xBim (int): Bin for x-axis.\n            xMin (float): Minimum of graph/histogram for x-axis.\n            xMaz (float): Maximum of graph/histogram for x-axis.\n            yBim (int): Bin for y-axis.\n            yMin (float): Minimum of graph/histogram for y-axis.\n            yMaz (float): Maximum of graph/histogram for y-axis.\n        \"\"\"\n        self.Titles = titles\n        self.SplitTitle()\n        self.HistXRange = (xBin, xMin, xMax)\n        self.HistYRange = (yBin, yMin, yMax)\n        # print(self.HistXRange)\n\n    def SplitTitle(self):\n        \"\"\"SplitTitle\n\n        Analysis function for graph labels. It is automatically called when the title labels is set.\n\n        Args:\n            None.\n        \"\"\"\n        Buf = self.Titles.split(sep=';')\n        # print(Buf)\n        if len(Buf) &gt;= 3:\n            self.TitleLabel = Buf[0]\n            self.xAxisLabel = Buf[1]\n            self.yAxisLabel = Buf[2]\n            if len(Buf) == 4: self.y2AxisLabel = Buf[3]\n\n    def DrawCanv(self, figsize: tuple=None):\n        \"\"\"DrawCanv\n\n        Draw a canvas.\n\n        Note:\n            If the canvas already exists, redraw it.\n        \"\"\"\n\n        if figsize is not None: self.FigSize=figsize\n        if self.Fig is not None: self.Fig = None\n        self.Fig = plt.figure(figsize=self.FigSize)\n\n    def Draw1D(self, data):\n        \"\"\"Draw1D\n\n        Draw a 1-Dim histogram. \n\n        Args:\n            data (array): value of data.\n        \"\"\"\n        self.DrawCanv()\n        if len(self.Axs) &gt; 0: self.Axs.clear()\n        self.Axs.append(self.Fig.add_subplot(1,1,1))\n        # print(self.Axs)\n        # print(len(self.Axs))\n        # print(self.HistXRange)\n        # print(self.xAxisLabel)\n\n        self.Axs[0].hist(data, bins=self.HistXRange[0], range=self.HistXRange[1:], histtype='step')\n        self.Axs[0].set_xlim(self.HistXRange[1:])\n        self.Axs[0].set_title(self.TitleLabel)\n        self.Axs[0].set_xlabel(self.xAxisLabel, loc='right')\n        self.Axs[0].set_ylabel(self.yAxisLabel, loc='top')\n\n        return self.Axs[0]\n\n    def Draw2D(self, x, y):\n        \"\"\"Draw2D\n\n        Draw a 2-Dim histogram. Automatic generation of color bars.\n\n        Args:\n            x (array): value of data for x-axis hist.\n            y (array): value of data for y-axis hist.\n        \"\"\"\n        self.DrawCanv()\n        self.Axs.append(self.Fig.add_subplot(1,1,1))\n        Hist = self.Axs[0].hist2d(x, y,\n                                bins=(self.HistXRange[0], self.HistYRange[0]), \n                                range=(self.HistXRange[1:],self.HistYRange[1:]))\n        self.Fig.colorbar(Hist[3], ax=self.Axs[0])\n\n        self.Axs[0].set_title(self.TitleLabel)\n        self.Axs[0].set_xlabel(self.xAxisLabel, loc='right')\n        self.Axs[0].set_ylabel(self.yAxisLabel, loc='top')\n        pass\n\n    def DrawSep2(self, x, ax0_y, ax1_y, ratio=0.8, val_hline=None):\n        \"\"\"DrawSep2\n\n        Draw a graphs with the canvas divided into two parts by an division ratio; the x-axis is shared.\n\n        Args:\n            x (array): value of data for x-axis hist.\n            ax0_y (array): value of data for upper graph y-axis.\n            ax1_y (array): value of data for lower graph y-axis.\n            ratio (float): division ratio to the y length of the canvas. (default: 0.8)\n            val_hline (bool): To enable flag for y=0 horizontal line in the lower graph.\n\n        Examples:\n            The function has been implemented to show the variance of the data with respect to the fitted graph and the fitted curve\n        \"\"\"\n        self.DrawCanv()\n        if len(self.Axs) &gt; 0: self.Axs.clear()\n        self.Axs = self.Fig.subplots(2, 1)\n        plt.subplots_adjust(wspace=0, hspace=0)\n\n        self.Axs[0].plot(x, ax0_y, marker='o', linestyle='', color='black')\n        self.Axs[1].plot(x, ax1_y, marker='o', linestyle='', color='black')\n\n        if val_hline is not None:\n            self.Axs[1].axhline(y=val_hline, linestyle='--', color='red')\n\n        self.Axs[0].set_title(self.TitleLabel)\n        self.Axs[1].set_xlabel(self.xAxisLabel, loc='right')\n        self.Axs[0].set_ylabel(self.yAxisLabel, loc='top')\n        self.Axs[1].set_ylabel(self.y2AxisLabel, loc='top')\n\n        self.Fig.align_labels()\n        return self.Axs\n\n    def SetxLimit(self, lower, upper):\n        \"\"\"SetxLimit\n\n        Set the viewing range for the x-axis from lower val. to upper val.\n\n        Args:\n            index (int): index of graph. index 0 is upper, 1 is lower.\n            lower (float): lower limit val.\n            upper (float): upper limit val.\n        Note:\n            If an out-of-range index is used, this function is ignored.\n        \"\"\"\n        for ax in self.Axs:\n            ax.set_xlim(lower, upper)\n\n    def SetyLimit(self, index, lower, upper):\n        \"\"\"SetyLimit\n\n        Set the viewing range for the y-axis from lower val. to upper val.\n\n        Args:\n            index (int): index of graph. index 0 is upper, 1 is lower.\n            lower (float): lower limit val.\n            upper (float): upper limit val.\n        Note:\n            If an out-of-range index is used, this function is ignored.\n        \"\"\"\n        if index &lt; len(self.Axs):\n            self.Axs[index].set_ylim(lower, upper)\n        else:\n            print('index is out of range.')\n\n    def Print(self, path='./plot.pdf', dpi=350, pad_inches=0.5):\n        \"\"\"Print\n\n        Save the graph.\n\n        Args:\n            path (str): path to file.\n            dpi (int): DPI.\n            pad_inches (float): pad inches.\n        \"\"\"\n        plt.savefig(path, dpi=dpi, bbox_inches='tight', pad_inches=pad_inches)\n\n    def Clear(self):\n        \"\"\"Clear\n\n        Clear the pad.\n        \"\"\"\n        plt.cla()\n        plt.clf()\n</code></pre>"},{"location":"api/kplot/#kplot.kplot.hist.Clear","title":"<code>Clear()</code>","text":"<p>Clear</p> <p>Clear the pad.</p> Source code in <code>kplot\\kplot.py</code> <pre><code>def Clear(self):\n    \"\"\"Clear\n\n    Clear the pad.\n    \"\"\"\n    plt.cla()\n    plt.clf()\n</code></pre>"},{"location":"api/kplot/#kplot.kplot.hist.Draw1D","title":"<code>Draw1D(data)</code>","text":"<p>Draw1D</p> <p>Draw a 1-Dim histogram. </p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>array</code> <p>value of data.</p> required Source code in <code>kplot\\kplot.py</code> <pre><code>def Draw1D(self, data):\n    \"\"\"Draw1D\n\n    Draw a 1-Dim histogram. \n\n    Args:\n        data (array): value of data.\n    \"\"\"\n    self.DrawCanv()\n    if len(self.Axs) &gt; 0: self.Axs.clear()\n    self.Axs.append(self.Fig.add_subplot(1,1,1))\n    # print(self.Axs)\n    # print(len(self.Axs))\n    # print(self.HistXRange)\n    # print(self.xAxisLabel)\n\n    self.Axs[0].hist(data, bins=self.HistXRange[0], range=self.HistXRange[1:], histtype='step')\n    self.Axs[0].set_xlim(self.HistXRange[1:])\n    self.Axs[0].set_title(self.TitleLabel)\n    self.Axs[0].set_xlabel(self.xAxisLabel, loc='right')\n    self.Axs[0].set_ylabel(self.yAxisLabel, loc='top')\n\n    return self.Axs[0]\n</code></pre>"},{"location":"api/kplot/#kplot.kplot.hist.Draw2D","title":"<code>Draw2D(x, y)</code>","text":"<p>Draw2D</p> <p>Draw a 2-Dim histogram. Automatic generation of color bars.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>array</code> <p>value of data for x-axis hist.</p> required <code>y</code> <code>array</code> <p>value of data for y-axis hist.</p> required Source code in <code>kplot\\kplot.py</code> <pre><code>def Draw2D(self, x, y):\n    \"\"\"Draw2D\n\n    Draw a 2-Dim histogram. Automatic generation of color bars.\n\n    Args:\n        x (array): value of data for x-axis hist.\n        y (array): value of data for y-axis hist.\n    \"\"\"\n    self.DrawCanv()\n    self.Axs.append(self.Fig.add_subplot(1,1,1))\n    Hist = self.Axs[0].hist2d(x, y,\n                            bins=(self.HistXRange[0], self.HistYRange[0]), \n                            range=(self.HistXRange[1:],self.HistYRange[1:]))\n    self.Fig.colorbar(Hist[3], ax=self.Axs[0])\n\n    self.Axs[0].set_title(self.TitleLabel)\n    self.Axs[0].set_xlabel(self.xAxisLabel, loc='right')\n    self.Axs[0].set_ylabel(self.yAxisLabel, loc='top')\n    pass\n</code></pre>"},{"location":"api/kplot/#kplot.kplot.hist.DrawCanv","title":"<code>DrawCanv(figsize=None)</code>","text":"<p>DrawCanv</p> <p>Draw a canvas.</p> Note <p>If the canvas already exists, redraw it.</p> Source code in <code>kplot\\kplot.py</code> <pre><code>def DrawCanv(self, figsize: tuple=None):\n    \"\"\"DrawCanv\n\n    Draw a canvas.\n\n    Note:\n        If the canvas already exists, redraw it.\n    \"\"\"\n\n    if figsize is not None: self.FigSize=figsize\n    if self.Fig is not None: self.Fig = None\n    self.Fig = plt.figure(figsize=self.FigSize)\n</code></pre>"},{"location":"api/kplot/#kplot.kplot.hist.DrawSep2","title":"<code>DrawSep2(x, ax0_y, ax1_y, ratio=0.8, val_hline=None)</code>","text":"<p>DrawSep2</p> <p>Draw a graphs with the canvas divided into two parts by an division ratio; the x-axis is shared.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>array</code> <p>value of data for x-axis hist.</p> required <code>ax0_y</code> <code>array</code> <p>value of data for upper graph y-axis.</p> required <code>ax1_y</code> <code>array</code> <p>value of data for lower graph y-axis.</p> required <code>ratio</code> <code>float</code> <p>division ratio to the y length of the canvas. (default: 0.8)</p> <code>0.8</code> <code>val_hline</code> <code>bool</code> <p>To enable flag for y=0 horizontal line in the lower graph.</p> <code>None</code> <p>Examples:</p> <p>The function has been implemented to show the variance of the data with respect to the fitted graph and the fitted curve</p> Source code in <code>kplot\\kplot.py</code> <pre><code>def DrawSep2(self, x, ax0_y, ax1_y, ratio=0.8, val_hline=None):\n    \"\"\"DrawSep2\n\n    Draw a graphs with the canvas divided into two parts by an division ratio; the x-axis is shared.\n\n    Args:\n        x (array): value of data for x-axis hist.\n        ax0_y (array): value of data for upper graph y-axis.\n        ax1_y (array): value of data for lower graph y-axis.\n        ratio (float): division ratio to the y length of the canvas. (default: 0.8)\n        val_hline (bool): To enable flag for y=0 horizontal line in the lower graph.\n\n    Examples:\n        The function has been implemented to show the variance of the data with respect to the fitted graph and the fitted curve\n    \"\"\"\n    self.DrawCanv()\n    if len(self.Axs) &gt; 0: self.Axs.clear()\n    self.Axs = self.Fig.subplots(2, 1)\n    plt.subplots_adjust(wspace=0, hspace=0)\n\n    self.Axs[0].plot(x, ax0_y, marker='o', linestyle='', color='black')\n    self.Axs[1].plot(x, ax1_y, marker='o', linestyle='', color='black')\n\n    if val_hline is not None:\n        self.Axs[1].axhline(y=val_hline, linestyle='--', color='red')\n\n    self.Axs[0].set_title(self.TitleLabel)\n    self.Axs[1].set_xlabel(self.xAxisLabel, loc='right')\n    self.Axs[0].set_ylabel(self.yAxisLabel, loc='top')\n    self.Axs[1].set_ylabel(self.y2AxisLabel, loc='top')\n\n    self.Fig.align_labels()\n    return self.Axs\n</code></pre>"},{"location":"api/kplot/#kplot.kplot.hist.Print","title":"<code>Print(path='./plot.pdf', dpi=350, pad_inches=0.5)</code>","text":"<p>Print</p> <p>Save the graph.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>path to file.</p> <code>'./plot.pdf'</code> <code>dpi</code> <code>int</code> <p>DPI.</p> <code>350</code> <code>pad_inches</code> <code>float</code> <p>pad inches.</p> <code>0.5</code> Source code in <code>kplot\\kplot.py</code> <pre><code>def Print(self, path='./plot.pdf', dpi=350, pad_inches=0.5):\n    \"\"\"Print\n\n    Save the graph.\n\n    Args:\n        path (str): path to file.\n        dpi (int): DPI.\n        pad_inches (float): pad inches.\n    \"\"\"\n    plt.savefig(path, dpi=dpi, bbox_inches='tight', pad_inches=pad_inches)\n</code></pre>"},{"location":"api/kplot/#kplot.kplot.hist.SetxLimit","title":"<code>SetxLimit(lower, upper)</code>","text":"<p>SetxLimit</p> <p>Set the viewing range for the x-axis from lower val. to upper val.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>int</code> <p>index of graph. index 0 is upper, 1 is lower.</p> required <code>lower</code> <code>float</code> <p>lower limit val.</p> required <code>upper</code> <code>float</code> <p>upper limit val.</p> required <p>Note:     If an out-of-range index is used, this function is ignored.</p> Source code in <code>kplot\\kplot.py</code> <pre><code>def SetxLimit(self, lower, upper):\n    \"\"\"SetxLimit\n\n    Set the viewing range for the x-axis from lower val. to upper val.\n\n    Args:\n        index (int): index of graph. index 0 is upper, 1 is lower.\n        lower (float): lower limit val.\n        upper (float): upper limit val.\n    Note:\n        If an out-of-range index is used, this function is ignored.\n    \"\"\"\n    for ax in self.Axs:\n        ax.set_xlim(lower, upper)\n</code></pre>"},{"location":"api/kplot/#kplot.kplot.hist.SetyLimit","title":"<code>SetyLimit(index, lower, upper)</code>","text":"<p>SetyLimit</p> <p>Set the viewing range for the y-axis from lower val. to upper val.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>int</code> <p>index of graph. index 0 is upper, 1 is lower.</p> required <code>lower</code> <code>float</code> <p>lower limit val.</p> required <code>upper</code> <code>float</code> <p>upper limit val.</p> required <p>Note:     If an out-of-range index is used, this function is ignored.</p> Source code in <code>kplot\\kplot.py</code> <pre><code>def SetyLimit(self, index, lower, upper):\n    \"\"\"SetyLimit\n\n    Set the viewing range for the y-axis from lower val. to upper val.\n\n    Args:\n        index (int): index of graph. index 0 is upper, 1 is lower.\n        lower (float): lower limit val.\n        upper (float): upper limit val.\n    Note:\n        If an out-of-range index is used, this function is ignored.\n    \"\"\"\n    if index &lt; len(self.Axs):\n        self.Axs[index].set_ylim(lower, upper)\n    else:\n        print('index is out of range.')\n</code></pre>"},{"location":"api/kplot/#kplot.kplot.hist.SplitTitle","title":"<code>SplitTitle()</code>","text":"<p>SplitTitle</p> <p>Analysis function for graph labels. It is automatically called when the title labels is set.</p> Source code in <code>kplot\\kplot.py</code> <pre><code>def SplitTitle(self):\n    \"\"\"SplitTitle\n\n    Analysis function for graph labels. It is automatically called when the title labels is set.\n\n    Args:\n        None.\n    \"\"\"\n    Buf = self.Titles.split(sep=';')\n    # print(Buf)\n    if len(Buf) &gt;= 3:\n        self.TitleLabel = Buf[0]\n        self.xAxisLabel = Buf[1]\n        self.yAxisLabel = Buf[2]\n        if len(Buf) == 4: self.y2AxisLabel = Buf[3]\n</code></pre>"},{"location":"api/kplot/#kplot.kplot.hist.__init__","title":"<code>__init__(titles, xBin=10, xMin=0, xMax=1, yBin=0, yMin=0, yMax=0)</code>","text":"<p>default constructor</p> <p>default constructor</p> <p>Parameters:</p> Name Type Description Default <code>titles</code> <code>str</code> <p>The titles of the graph. \"Graph title;x-axis label;y-axis label\".</p> required <code>xBim</code> <code>int</code> <p>Bin for x-axis.</p> required <code>xMin</code> <code>float</code> <p>Minimum of graph/histogram for x-axis.</p> <code>0</code> <code>xMaz</code> <code>float</code> <p>Maximum of graph/histogram for x-axis.</p> required <code>yBim</code> <code>int</code> <p>Bin for y-axis.</p> required <code>yMin</code> <code>float</code> <p>Minimum of graph/histogram for y-axis.</p> <code>0</code> <code>yMaz</code> <code>float</code> <p>Maximum of graph/histogram for y-axis.</p> required Source code in <code>kplot\\kplot.py</code> <pre><code>def __init__(self, titles, xBin=10, xMin=0, xMax=1, yBin=0, yMin=0, yMax=0):\n    \"\"\"default constructor\n\n    default constructor\n\n    Args:\n        titles (str): The titles of the graph. \"Graph title;x-axis label;y-axis label\".\n        xBim (int): Bin for x-axis.\n        xMin (float): Minimum of graph/histogram for x-axis.\n        xMaz (float): Maximum of graph/histogram for x-axis.\n        yBim (int): Bin for y-axis.\n        yMin (float): Minimum of graph/histogram for y-axis.\n        yMaz (float): Maximum of graph/histogram for y-axis.\n    \"\"\"\n    self.Titles = titles\n    self.SplitTitle()\n    self.HistXRange = (xBin, xMin, xMax)\n    self.HistYRange = (yBin, yMin, yMax)\n</code></pre>"},{"location":"api/kplot/#scatter","title":"Scatter","text":"<p>kplot.scatter3</p> <ul> <li>The 2D histogram/graph with histograms of each axis component using Matplotlib. It's beta version. We will add the ability to manage common parameters.</li> </ul>"},{"location":"api/kplot/#kplot.kscatter.scatter3","title":"<code>scatter3</code>","text":"<p>kplot.scatter3 class</p> <p>Create a one or two dimension graph/histogram.</p> Source code in <code>kplot\\kscatter.py</code> <pre><code>class scatter3:\n    \"\"\"kplot.scatter3 class\n\n    Create a one or two dimension graph/histogram.\n\n    \"\"\"\n    Fig = None\n    Ax  = []\n    HistXRange=None\n    HistYRange=None\n    PadMin = 0.1\n    PadMax = 0.2\n    wSpace = (0.12, 0.14)\n\n    Mode = 'hist'\n    valX = None\n    valY = None\n\n    def __init__(self, xBin=None, xMin=None, xMax=None, yBin=None, yMin=None, yMax=None, ratio=0.6, figsize=(8.27, 8.27)) -&gt; None:\n        \"\"\"default constructor\n\n        default constructor\n\n        Args:\n            xBim (int): Bin for x-axis.\n            xMin (float): Minimum of graph/histogram for x-axis.\n            xMaz (float): Maximum of graph/histogram for x-axis.\n            yBim (int): Bin for y-axis.\n            yMin (float): Minimum of graph/histogram for y-axis.\n            yMaz (float): Maximum of graph/histogram for y-axis.\n            ratio (float): division ratio for graph and histograms in canvas.\n            figsize (float): canvas size in inch.\n        \"\"\"\n        plt.rcParams[\"font.size\"] = 9\n        self.SetRatio(ratio, figsize)\n\n        if self.HistXRange is not None: self.HistXRange = None\n        if self.HistYRange is not None: self.HistYRange = None\n\n        if xBin is not None:\n            self.HistXRange=(xBin, xMin, xMax)\n        if yBin is not None:\n            self.HistYRange=(yBin, yMin, yMax)\n\n    def SetRatio(self, ratio=0.6, figsize=(8.27, 8.27)):\n        \"\"\"SetRatio\n\n        Set a canvas size and a division ratio and for graph and histograms in canvas.\n\n        Args:\n            ratio (float): division ratio for graph and histograms in canvas.\n            figsize (float): canvas size in inch.\n\n        Returns:\n            self.Ax: axis object of matplotlib\n        \"\"\"\n        if self.Fig is not None: self.Fig = None\n        self.Fig = plt.figure(figsize=figsize)\n\n        if len(self.Ax)&gt;0: self.Ax.clear()\n\n        Min = self.PadMin\n        Max = self.PadMax\n        wSpace= self.wSpace\n\n        wScatter   = (Min, Min, ratio, ratio)\n        wHistUpper = (Min, ratio+wSpace[0], ratio, Max)\n        wHistSide  = (ratio+wSpace[1], Min, Max, ratio)\n\n        self.Ax.append(plt.axes(wScatter))\n        self.Ax.append(plt.axes(wHistUpper))\n        self.Ax.append(plt.axes(wHistSide))\n\n        self.Ax[1].set_xticklabels([])\n        self.Ax[2].set_yticklabels([])\n\n        return self.Ax\n    def GetFig(self):\n        \"\"\"GetFig\n\n        Get the figure object.\n\n        Returns:\n            self.Fig: figure object of matplotlib\n        \"\"\"\n        return self.Fig\n\n    def GetAxis(self):\n        \"\"\"GetAxis\n\n        Get the axis object.\n\n        Returns:\n            self.Ax: axis object of matplotlib\n        \"\"\"\n        return self.Ax\n\n    def SetxLimit(self, x_min: float=None, x_max: float=None, x_range: tuple = None):\n        \"\"\"SetxLabel\n\n        Set the x-axis limit for histograms and graphs. There are two methods to set limits.\n        The primary method uses parameters `x_mix` and `x_max` . The secondary method sets the range of the x-axis limit using a tuple `x_range`.\n\n        Args:\n            x_min (float): lower limit.\n            x_max (float): upper limit.\n            x_range (tuple): limit range `(x_min, x_max)`\n\n        Note:\n            If all parameters is set, use the value of `x_range`.\n\n        \"\"\"\n        Val = None\n        if x_min is not None and x_max is not None:\n            Val = (x_min, x_max)\n        if x_range is not None:\n            Val = x_range\n\n        self.Ax[0].set_xlim(Val[0], Val[1])\n        self.Ax[1].set_xlim(Val[0], Val[1])\n\n    def SetyLimit(self, y_min: float=None, y_max: float=None, y_range: tuple = None):\n        Val = None\n        if y_min is not None and y_max is not None:\n            Val = (y_min, y_max)\n        if y_range is not None:\n            Val = y_range\n\n        self.Ax[0].set_ylim(Val[0], Val[1])\n        self.Ax[2].set_ylim(Val[0], Val[1])\n\n    def SetxLabel(self, title):\n        \"\"\"SetxLabel\n\n        Set the x-axis label for main histograms/graph pad.\n\n        Args:\n            title (str): label title.\n        \"\"\"\n        self.Ax[0].set_xlabel(title, loc='right')\n\n    def SetyLabel(self, title):\n        \"\"\"SetyLabel\n\n        Set the y-axis label for main histograms/graph pad.\n\n        Args:\n            title (str): label title.\n        \"\"\"\n        self.Ax[0].set_ylabel(title, loc='top')\n        pass\n\n    def SetHistLabel(self, title='Events', axis='all'):\n        \"\"\"SetHistLabel\n\n        Set the labels for x-/y- axis histograms.\n\n        Args:\n            title (str): label title.\n            axis (str): Tag for the histogram index. default: 'all', set y-axis labels for all axis histograms with a common title. you can use 'upper' or 'lower' paras.\n        \"\"\"\n        if axis == 'all' or axis == 'upper':\n            self.Ax[1].set_ylabel(title, loc='top')\n        if axis == 'all' or axis == 'side':\n            self.Ax[2].set_xlabel(title, loc='right')\n\n    def Draw(self, x, y, mode=None, marker_labels: list=None, norm=None):\n        \"\"\"Draw\n\n        Draw the graphs.\n\n        Args:\n            x (array): value of data for x-axis hist/graph.\n            y (array): value of data for y-axis hist/graph.\n            mode (str): plot mode. you can used 'scatter' or 'hist'\n            marker_labels (str list): Marker labels. Only valid in scatter mode.\n            norm (str or obj): enable log scaling for z-axis. Only valid in hist mode.\n        \"\"\"\n        if mode is not None: self.Mode = mode\n        Znorm = norm\n\n        self.valX = x\n        self.valY = y\n        # print(x)\n        # print(self.valX)\n        # print(y)\n        # print(self.valY)\n\n        if self.Mode == 'scatter':\n            self.Ax[0].scatter(x, y, color='black', marker='o')\n            if marker_labels is not None:\n                for i, txt in enumerate(marker_labels):\n                    Text = self.Ax[0].annotate(txt, (x[i], y[i]), fontsize=plt.rcParams[\"font.size\"]*0.8, textcoords='offset points', xytext=(-4, 5))\n        elif self.Mode == 'hist':\n                # ZScale = None\n                # if z_scale: ZScale = colors.LogNorm()\n                if norm == 'log' or norm == True:\n                    Znorm = colors.LogNorm()\n                    # print('log scale')\n\n                self.Ax[0].hist2d(x, y, bins=(self.HistXRange[0], self.HistYRange[0]), range=(self.HistXRange[1:], self.HistYRange[1:]), norm=Znorm)\n\n        self.Ax[1].hist(x, bins=self.HistXRange[0], range=self.HistXRange[1:], color='black', histtype='step')\n        self.Ax[2].hist(y, bins=self.HistYRange[0], range=self.HistYRange[1:], color='black', histtype='step', orientation='horizontal')\n\n        self.SetHistLabel()\n        self.Fig.align_labels(axs=self.Ax)\n\n        return self.Ax\n\n    def SetLogz(self, flag=True):\n        \"\"\"SetLogz\n\n        Set or unset the log scale for z-axis.\n\n        Args:\n            flag (bool): Enable log scaling.\n        \"\"\"\n        Znorm = None\n        if self.Mode == 'hist':\n            if flag: Znorm = colors.LogNorm()\n            self.Ax[0].cla()\n            self.Draw(self.valX, self.valY, norm=Znorm)\n\n    def Print(self, path='./scatter.pdf', dpi=350, pad_inches=0.5):\n        \"\"\"Print\n\n        Save the graph.\n\n        Args:\n            path (str): path to file.\n            dpi (int): DPI.\n            pad_inches (float): pad inches.\n        \"\"\"\n        plt.savefig(path, dpi=dpi, bbox_inches='tight', pad_inches=pad_inches)\n\n    def Clear():\n        \"\"\"Clear\n\n        Clear the pad.\n        \"\"\"\n        plt.cla()\n        plt.clf()\n</code></pre>"},{"location":"api/kplot/#kplot.kscatter.scatter3.Clear","title":"<code>Clear()</code>","text":"<p>Clear</p> <p>Clear the pad.</p> Source code in <code>kplot\\kscatter.py</code> <pre><code>def Clear():\n    \"\"\"Clear\n\n    Clear the pad.\n    \"\"\"\n    plt.cla()\n    plt.clf()\n</code></pre>"},{"location":"api/kplot/#kplot.kscatter.scatter3.Draw","title":"<code>Draw(x, y, mode=None, marker_labels=None, norm=None)</code>","text":"<p>Draw</p> <p>Draw the graphs.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>array</code> <p>value of data for x-axis hist/graph.</p> required <code>y</code> <code>array</code> <p>value of data for y-axis hist/graph.</p> required <code>mode</code> <code>str</code> <p>plot mode. you can used 'scatter' or 'hist'</p> <code>None</code> <code>marker_labels</code> <code>str list</code> <p>Marker labels. Only valid in scatter mode.</p> <code>None</code> <code>norm</code> <code>str or obj</code> <p>enable log scaling for z-axis. Only valid in hist mode.</p> <code>None</code> Source code in <code>kplot\\kscatter.py</code> <pre><code>def Draw(self, x, y, mode=None, marker_labels: list=None, norm=None):\n    \"\"\"Draw\n\n    Draw the graphs.\n\n    Args:\n        x (array): value of data for x-axis hist/graph.\n        y (array): value of data for y-axis hist/graph.\n        mode (str): plot mode. you can used 'scatter' or 'hist'\n        marker_labels (str list): Marker labels. Only valid in scatter mode.\n        norm (str or obj): enable log scaling for z-axis. Only valid in hist mode.\n    \"\"\"\n    if mode is not None: self.Mode = mode\n    Znorm = norm\n\n    self.valX = x\n    self.valY = y\n    # print(x)\n    # print(self.valX)\n    # print(y)\n    # print(self.valY)\n\n    if self.Mode == 'scatter':\n        self.Ax[0].scatter(x, y, color='black', marker='o')\n        if marker_labels is not None:\n            for i, txt in enumerate(marker_labels):\n                Text = self.Ax[0].annotate(txt, (x[i], y[i]), fontsize=plt.rcParams[\"font.size\"]*0.8, textcoords='offset points', xytext=(-4, 5))\n    elif self.Mode == 'hist':\n            # ZScale = None\n            # if z_scale: ZScale = colors.LogNorm()\n            if norm == 'log' or norm == True:\n                Znorm = colors.LogNorm()\n                # print('log scale')\n\n            self.Ax[0].hist2d(x, y, bins=(self.HistXRange[0], self.HistYRange[0]), range=(self.HistXRange[1:], self.HistYRange[1:]), norm=Znorm)\n\n    self.Ax[1].hist(x, bins=self.HistXRange[0], range=self.HistXRange[1:], color='black', histtype='step')\n    self.Ax[2].hist(y, bins=self.HistYRange[0], range=self.HistYRange[1:], color='black', histtype='step', orientation='horizontal')\n\n    self.SetHistLabel()\n    self.Fig.align_labels(axs=self.Ax)\n\n    return self.Ax\n</code></pre>"},{"location":"api/kplot/#kplot.kscatter.scatter3.GetAxis","title":"<code>GetAxis()</code>","text":"<p>GetAxis</p> <p>Get the axis object.</p> <p>Returns:</p> Type Description <p>self.Ax: axis object of matplotlib</p> Source code in <code>kplot\\kscatter.py</code> <pre><code>def GetAxis(self):\n    \"\"\"GetAxis\n\n    Get the axis object.\n\n    Returns:\n        self.Ax: axis object of matplotlib\n    \"\"\"\n    return self.Ax\n</code></pre>"},{"location":"api/kplot/#kplot.kscatter.scatter3.GetFig","title":"<code>GetFig()</code>","text":"<p>GetFig</p> <p>Get the figure object.</p> <p>Returns:</p> Type Description <p>self.Fig: figure object of matplotlib</p> Source code in <code>kplot\\kscatter.py</code> <pre><code>def GetFig(self):\n    \"\"\"GetFig\n\n    Get the figure object.\n\n    Returns:\n        self.Fig: figure object of matplotlib\n    \"\"\"\n    return self.Fig\n</code></pre>"},{"location":"api/kplot/#kplot.kscatter.scatter3.Print","title":"<code>Print(path='./scatter.pdf', dpi=350, pad_inches=0.5)</code>","text":"<p>Print</p> <p>Save the graph.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>path to file.</p> <code>'./scatter.pdf'</code> <code>dpi</code> <code>int</code> <p>DPI.</p> <code>350</code> <code>pad_inches</code> <code>float</code> <p>pad inches.</p> <code>0.5</code> Source code in <code>kplot\\kscatter.py</code> <pre><code>def Print(self, path='./scatter.pdf', dpi=350, pad_inches=0.5):\n    \"\"\"Print\n\n    Save the graph.\n\n    Args:\n        path (str): path to file.\n        dpi (int): DPI.\n        pad_inches (float): pad inches.\n    \"\"\"\n    plt.savefig(path, dpi=dpi, bbox_inches='tight', pad_inches=pad_inches)\n</code></pre>"},{"location":"api/kplot/#kplot.kscatter.scatter3.SetHistLabel","title":"<code>SetHistLabel(title='Events', axis='all')</code>","text":"<p>SetHistLabel</p> <p>Set the labels for x-/y- axis histograms.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <code>str</code> <p>label title.</p> <code>'Events'</code> <code>axis</code> <code>str</code> <p>Tag for the histogram index. default: 'all', set y-axis labels for all axis histograms with a common title. you can use 'upper' or 'lower' paras.</p> <code>'all'</code> Source code in <code>kplot\\kscatter.py</code> <pre><code>def SetHistLabel(self, title='Events', axis='all'):\n    \"\"\"SetHistLabel\n\n    Set the labels for x-/y- axis histograms.\n\n    Args:\n        title (str): label title.\n        axis (str): Tag for the histogram index. default: 'all', set y-axis labels for all axis histograms with a common title. you can use 'upper' or 'lower' paras.\n    \"\"\"\n    if axis == 'all' or axis == 'upper':\n        self.Ax[1].set_ylabel(title, loc='top')\n    if axis == 'all' or axis == 'side':\n        self.Ax[2].set_xlabel(title, loc='right')\n</code></pre>"},{"location":"api/kplot/#kplot.kscatter.scatter3.SetLogz","title":"<code>SetLogz(flag=True)</code>","text":"<p>SetLogz</p> <p>Set or unset the log scale for z-axis.</p> <p>Parameters:</p> Name Type Description Default <code>flag</code> <code>bool</code> <p>Enable log scaling.</p> <code>True</code> Source code in <code>kplot\\kscatter.py</code> <pre><code>def SetLogz(self, flag=True):\n    \"\"\"SetLogz\n\n    Set or unset the log scale for z-axis.\n\n    Args:\n        flag (bool): Enable log scaling.\n    \"\"\"\n    Znorm = None\n    if self.Mode == 'hist':\n        if flag: Znorm = colors.LogNorm()\n        self.Ax[0].cla()\n        self.Draw(self.valX, self.valY, norm=Znorm)\n</code></pre>"},{"location":"api/kplot/#kplot.kscatter.scatter3.SetRatio","title":"<code>SetRatio(ratio=0.6, figsize=(8.27, 8.27))</code>","text":"<p>SetRatio</p> <p>Set a canvas size and a division ratio and for graph and histograms in canvas.</p> <p>Parameters:</p> Name Type Description Default <code>ratio</code> <code>float</code> <p>division ratio for graph and histograms in canvas.</p> <code>0.6</code> <code>figsize</code> <code>float</code> <p>canvas size in inch.</p> <code>(8.27, 8.27)</code> <p>Returns:</p> Type Description <p>self.Ax: axis object of matplotlib</p> Source code in <code>kplot\\kscatter.py</code> <pre><code>def SetRatio(self, ratio=0.6, figsize=(8.27, 8.27)):\n    \"\"\"SetRatio\n\n    Set a canvas size and a division ratio and for graph and histograms in canvas.\n\n    Args:\n        ratio (float): division ratio for graph and histograms in canvas.\n        figsize (float): canvas size in inch.\n\n    Returns:\n        self.Ax: axis object of matplotlib\n    \"\"\"\n    if self.Fig is not None: self.Fig = None\n    self.Fig = plt.figure(figsize=figsize)\n\n    if len(self.Ax)&gt;0: self.Ax.clear()\n\n    Min = self.PadMin\n    Max = self.PadMax\n    wSpace= self.wSpace\n\n    wScatter   = (Min, Min, ratio, ratio)\n    wHistUpper = (Min, ratio+wSpace[0], ratio, Max)\n    wHistSide  = (ratio+wSpace[1], Min, Max, ratio)\n\n    self.Ax.append(plt.axes(wScatter))\n    self.Ax.append(plt.axes(wHistUpper))\n    self.Ax.append(plt.axes(wHistSide))\n\n    self.Ax[1].set_xticklabels([])\n    self.Ax[2].set_yticklabels([])\n\n    return self.Ax\n</code></pre>"},{"location":"api/kplot/#kplot.kscatter.scatter3.SetxLabel","title":"<code>SetxLabel(title)</code>","text":"<p>SetxLabel</p> <p>Set the x-axis label for main histograms/graph pad.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <code>str</code> <p>label title.</p> required Source code in <code>kplot\\kscatter.py</code> <pre><code>def SetxLabel(self, title):\n    \"\"\"SetxLabel\n\n    Set the x-axis label for main histograms/graph pad.\n\n    Args:\n        title (str): label title.\n    \"\"\"\n    self.Ax[0].set_xlabel(title, loc='right')\n</code></pre>"},{"location":"api/kplot/#kplot.kscatter.scatter3.SetxLimit","title":"<code>SetxLimit(x_min=None, x_max=None, x_range=None)</code>","text":"<p>SetxLabel</p> <p>Set the x-axis limit for histograms and graphs. There are two methods to set limits. The primary method uses parameters <code>x_mix</code> and <code>x_max</code> . The secondary method sets the range of the x-axis limit using a tuple <code>x_range</code>.</p> <p>Parameters:</p> Name Type Description Default <code>x_min</code> <code>float</code> <p>lower limit.</p> <code>None</code> <code>x_max</code> <code>float</code> <p>upper limit.</p> <code>None</code> <code>x_range</code> <code>tuple</code> <p>limit range <code>(x_min, x_max)</code></p> <code>None</code> Note <p>If all parameters is set, use the value of <code>x_range</code>.</p> Source code in <code>kplot\\kscatter.py</code> <pre><code>def SetxLimit(self, x_min: float=None, x_max: float=None, x_range: tuple = None):\n    \"\"\"SetxLabel\n\n    Set the x-axis limit for histograms and graphs. There are two methods to set limits.\n    The primary method uses parameters `x_mix` and `x_max` . The secondary method sets the range of the x-axis limit using a tuple `x_range`.\n\n    Args:\n        x_min (float): lower limit.\n        x_max (float): upper limit.\n        x_range (tuple): limit range `(x_min, x_max)`\n\n    Note:\n        If all parameters is set, use the value of `x_range`.\n\n    \"\"\"\n    Val = None\n    if x_min is not None and x_max is not None:\n        Val = (x_min, x_max)\n    if x_range is not None:\n        Val = x_range\n\n    self.Ax[0].set_xlim(Val[0], Val[1])\n    self.Ax[1].set_xlim(Val[0], Val[1])\n</code></pre>"},{"location":"api/kplot/#kplot.kscatter.scatter3.SetyLabel","title":"<code>SetyLabel(title)</code>","text":"<p>SetyLabel</p> <p>Set the y-axis label for main histograms/graph pad.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <code>str</code> <p>label title.</p> required Source code in <code>kplot\\kscatter.py</code> <pre><code>def SetyLabel(self, title):\n    \"\"\"SetyLabel\n\n    Set the y-axis label for main histograms/graph pad.\n\n    Args:\n        title (str): label title.\n    \"\"\"\n    self.Ax[0].set_ylabel(title, loc='top')\n    pass\n</code></pre>"},{"location":"api/kplot/#kplot.kscatter.scatter3.__init__","title":"<code>__init__(xBin=None, xMin=None, xMax=None, yBin=None, yMin=None, yMax=None, ratio=0.6, figsize=(8.27, 8.27))</code>","text":"<p>default constructor</p> <p>default constructor</p> <p>Parameters:</p> Name Type Description Default <code>xBim</code> <code>int</code> <p>Bin for x-axis.</p> required <code>xMin</code> <code>float</code> <p>Minimum of graph/histogram for x-axis.</p> <code>None</code> <code>xMaz</code> <code>float</code> <p>Maximum of graph/histogram for x-axis.</p> required <code>yBim</code> <code>int</code> <p>Bin for y-axis.</p> required <code>yMin</code> <code>float</code> <p>Minimum of graph/histogram for y-axis.</p> <code>None</code> <code>yMaz</code> <code>float</code> <p>Maximum of graph/histogram for y-axis.</p> required <code>ratio</code> <code>float</code> <p>division ratio for graph and histograms in canvas.</p> <code>0.6</code> <code>figsize</code> <code>float</code> <p>canvas size in inch.</p> <code>(8.27, 8.27)</code> Source code in <code>kplot\\kscatter.py</code> <pre><code>def __init__(self, xBin=None, xMin=None, xMax=None, yBin=None, yMin=None, yMax=None, ratio=0.6, figsize=(8.27, 8.27)) -&gt; None:\n    \"\"\"default constructor\n\n    default constructor\n\n    Args:\n        xBim (int): Bin for x-axis.\n        xMin (float): Minimum of graph/histogram for x-axis.\n        xMaz (float): Maximum of graph/histogram for x-axis.\n        yBim (int): Bin for y-axis.\n        yMin (float): Minimum of graph/histogram for y-axis.\n        yMaz (float): Maximum of graph/histogram for y-axis.\n        ratio (float): division ratio for graph and histograms in canvas.\n        figsize (float): canvas size in inch.\n    \"\"\"\n    plt.rcParams[\"font.size\"] = 9\n    self.SetRatio(ratio, figsize)\n\n    if self.HistXRange is not None: self.HistXRange = None\n    if self.HistYRange is not None: self.HistYRange = None\n\n    if xBin is not None:\n        self.HistXRange=(xBin, xMin, xMax)\n    if yBin is not None:\n        self.HistYRange=(yBin, yMin, yMax)\n</code></pre>"},{"location":"api/kplot/#fitting","title":"Fitting","text":""},{"location":"api/kplot/#common-utilities","title":"Common utilities","text":"<p>kplot.common</p> <ul> <li>The base class of all graph and histograms.</li> </ul>"},{"location":"api/kplot/#kplot.common.common","title":"<code>common</code>","text":"<p>kplot.common</p> <p>The base class of all graphs and histograms. This class manages parameters and function for graphs or histograms that involve generating the canvas.</p> Source code in <code>kplot\\common.py</code> <pre><code>class common:\n    \"\"\"kplot.common\n\n    The base class of all graphs and histograms. This class manages parameters and function for graphs or histograms that involve generating the canvas.\n\n    \"\"\"\n\n    # Paras \n    Fig = None\n    Ax = None\n\n    # function\n    def __init__(self) -&gt; None:\n        pass\n</code></pre>"},{"location":"api/kplot/#style","title":"Style","text":""}]}